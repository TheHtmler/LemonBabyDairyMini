---
description: 食物 v2 上线后提醒重建 daily_summary_v2
globs: "{miniprogram/utils/dailyRecordV2Service.js,miniprogram/utils/dailySummaryV2Utils.js,miniprogram/models/foodIntakeRecord.js,miniprogram/pages/meal-editor-v2/**,cloudfunctions/**/index.js,docs/miniprogram/**}"
alwaysApply: false
---

# 食物 v2 上线后的汇总清洗提醒

- 当前奶粉 v2 过渡期，`daily_summary_v2` 的食物部分仍可能来自旧 `feeding_records.intakes`。
- 当食物 v2 / `food_intake_records` 正式上线并替换旧食物入口后，必须规划一次 `daily_summary_v2` 统一重建/清洗。
- 重建时应使用稳定后的事实来源：`feeding_records_v2`、`food_intake_records`、`growth_records_v2`、`medication_records`、`treatment_records`、`bowel_records`。
- 清洗前保留 dry-run、按 `babyUid` / 日期范围 / 分批参数，并确认不再混用旧 `feeding_records.intakes`。
