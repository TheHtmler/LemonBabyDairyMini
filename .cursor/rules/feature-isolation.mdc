---
description: 新功能和大改动优先使用独立入口灰度验证
alwaysApply: true
---

# 新功能隔离策略

- 当新增功能、扩展已有能力，或对旧功能的交互/数据结构改动较大时，优先新建入口、新页面或 v2 流程进行验证。
- 不要直接替换线上稳定入口，除非用户明确要求替换。
- 新开的菜单、页面和 v2 流程，数据读写必须和旧版本隔离：不要读取旧集合作为回退，也不要写入旧集合，除非用户明确要求迁移或兼容。
- v2 功能验证齐全后会替换 v1；开发 v2 时优先使用新的集合、新字段或新数据结构，不为未完成的 v2 流程叠加旧数据兼容逻辑。
- 等新入口验证稳定后，再规划旧入口替换、迁移和清理。

